apiVersion: v1
kind: Service
metadata:
  name: cap-processamento
  namespace: cap-service
  labels:
    app: cap-processamento
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 4004
      protocol: TCP
      name: http
  selector:
    app: cap-processamento
